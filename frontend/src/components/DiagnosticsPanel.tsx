// DiagnosticsPanel removed: former metrics UI depended on /metrics/basic.
// Minimal stub to avoid runtime errors if something still imports it.
export default function DiagnosticsPanel() {
  return null;
}
